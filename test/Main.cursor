let foo = 50 / 2;

let person = {
    name: "John",
    age: 30,
    isMonster: true,
    address: {
        pincode: 123456,
        countryCode: 12
    }
};

print(foo, 45, time())

fn makeAdder(offset){
    fn add(x, y) {
        x + y + offset
    }
}

fn sub(x, y) {
    fn add(x, y) {
        let result = x + y;
        print(result)
        
        result
    }

    let foo = 35;
}

const adder = makeAdder(10);

const result = adder(10, 4);

print(result)

help()

const currentWorkingDir = dir;
print("Current working directory: ", currentWorkingDir)

print(print)

print(person.name)
print(person["age"])
let key = "isMonster";
print(person[key])
// print(person.address.pincode)

print(person[key])

const otp = rand(100000, 999999);

print("Your OTP is", otp, "and it is of length", len(str(otp)))

print(typeof(otp))

print(len("Hello World"))

print({
    firstName: "John",
    lastName: "Doe",
    middleName: "Smith",
    age: 30,
    isMonster: true,
    address: {
        pincode: 123456,
        countryCode: 12
    }
})

print(concat("John", "Doe", "Smith"))
